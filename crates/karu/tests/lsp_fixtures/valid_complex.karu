// Complex expressions

allow editor if
    principal == "alice" or
    principal == "bob";

deny restricted if
    action == "delete" and
    not principal == "admin";

allow search if
    "viewer" in principal.roles;

allow bulk_read if
    resource.items != null and
    forall item in resource.items: item.public == true;
