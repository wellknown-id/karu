Miscellaneous TODOs:

- [ ] Consider moving rust sources into meaningful crates.
- [ ] File format autodetection in CLI. Each third party language that is supported should have provide file extension(s) so the CLI knows what to report to the user it can handle. PRobably a trait required for this.
- [x] Check what all these .rs files are for. It feels like we have files that aren't used by anyone for anything! - Done!
